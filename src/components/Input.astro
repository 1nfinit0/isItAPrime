---
---

<div class="flex gap-2 w-full justify-center items-center">
  <textarea
    name="primeInput"
    id="primeInput"
    placeholder="Enter a number to check if it's prime"
    class="w-full p-2 rounded-2xl resize-none max-h-100 overflow-y-hidden focus:outline-none focus:ring-2 focus:ring-blue-500 bg-gray-800 text-gray-200"
  ></textarea>
  <button id="primeButton" class="px-4 py-2 bg-blue-500 text-white rounded">→</button>
</div>

<script>
  const textarea = document.getElementById("primeInput");
  import { autoResizeTextarea } from "@libs/textArea";
  autoResizeTextarea(textarea);
</script>
